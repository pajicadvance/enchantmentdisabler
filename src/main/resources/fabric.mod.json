{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",
  "name": "${name}",
  "description": "${description}",
  "authors": ["${authors}"],
  "contributors": ["${contributors}"],
  "contact": {
    "sources": "${sources_url}",
    "issues": "${issues_url}",
    "homepage": "${homepage_url}"
  },
  "custom": {
    "modmenu": {
      "links": {
        "modmenu.discord": "${discord_url}"
      }
    }
  },
  "license": "${license}",
  "icon": "icon.png",
  "environment": "*",
  "entrypoints": {},
  "mixins": [
    "${id}.mixins.json"
  ],
  "depends": {},
  "suggests": {}
}
